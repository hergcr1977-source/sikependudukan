/**
 * Database Initialization for Multi-RT support
 *
 * FILE INI SUDAH TIDAK DIPAKAI LAGI.
 * Migration sekarang ditangani oleh src/lib/db-migrate.ts yang menggunakan Prisma + PostgreSQL.
 *
 * File ini disimpan sebagai referensi saja.
 * Jika kamu butuh migration manual, jalankan:
 *   npx prisma db push
 * atau akses endpoint: /api/setup-db
 */
